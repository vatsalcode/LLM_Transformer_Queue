#include<bits/stdc++.h>
using namespace std;
int main()
{
    int num,flag=-1;
    cin>>num;
    for(int i=1; i<=10; i++)
    if(num%i == 0)
    {
        if(i>=flag)
        flag=i;
    }
    cout<<flag;
    return(0);
}
