#include<iostream>
#include<math.h>
using namespace std;
int main()
{
	int t,n;
	cin>>t;
	while(t--)
  {
	   cin>>n; 
	 int x = (n/2);
	 string s;
	 s.push_back('1');
	  for(int i=0;i<(n/2);i++)
    {
	    s.push_back('0');
	  }
	 cout<<"1 "<<s<<endl;
	 s.clear();
	}
	return 0;
}
