#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
	int n,h,x;
	cin>>n>>h>>x;
	int a[n];
	for(int i=0;i<n;i++)
	cin>>a[i];
	if(*max_element(a,a+n)<(h-x))
	cout<<"NO";
	else
	cout<<"YES";
	return 0;
}
