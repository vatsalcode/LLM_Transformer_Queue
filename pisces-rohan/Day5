//to count digits in a number

#include <stdio.h>

int main()
{
    int number;
    int a = 0;
    int count = 1;
    scanf("%d", &number);
    while (number / 10 != 0)
    {
        a = number / 10;
        count++;
        number = a;
    }
    printf("%d", count);
}
