int factorial(int n){
if(n==0)
return 1;

else
return (n*factorial(n-1));
}

int main(){

        int n;
        cin>>n;
        
        long = factorial(n);
        cout<<"Factorial of "<<n<<" is "<<fact;
        
        factorial(n;
}
