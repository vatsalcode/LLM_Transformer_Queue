Find the contiguous sub-array with maximum sum.

for(int i=0;i<n;i++)
{  m1 = m1 + arr[i]
   if(m1 > m2) m2 = m1
   if(m1 < 0) m1 = 0   }

Reverse a linked list

cur = head
while (cur != NULL)
{   next = cur->next
    cur->next = prev
    prev = cur
    cur = next   }
head = prev

Check if string is pallindrome

Python: a = a[::-1]

l = 0, r = n-1
a = s
while(l <= r)
{  t = s[l]
   s[l++] = s[r]
   s[r--] = s[l]
}
if(s==a) return true

