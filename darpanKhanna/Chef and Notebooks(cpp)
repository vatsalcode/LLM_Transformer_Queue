#include <iostream>
using namespace std;

int main() {
	long long int t;
	cin>>t;
	while(t--)
	{
	    long long int x, y, k, n, flag=0;
	    cin>>x>>y>>k>>n;
	    while(n--)
	    {
	        long long int p, c;
	        cin>>p>>c;
	        if(p >= x-y && c<=k)
	           flag=1;
	    }
	    if(flag == 1)
	    cout<<"LuckyChef"<<"\n";
	    else
	    cout<<"UnluckyChef"<<"\n";
	}
	return 0;
}
